using System;
using etcetera;

namespace Agora.Config
{
    public static class DiscoveryTool
    {
        private static readonly TimeSpan TimeToLive = TimeSpan.FromSeconds(5);

        // TODO(colemick): better types for `ipAddress`
        public static RegisterService(string machineFunction, Dictionary<string, string> )
        {

        }

        // TODO(colemick): Partitioning/Sharding design

    }
}
